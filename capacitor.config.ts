import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MAQUETACION_PAYPHONE_ADMIN_DASHBOARD_IONIC',
  webDir: 'www'
};

export default config;
